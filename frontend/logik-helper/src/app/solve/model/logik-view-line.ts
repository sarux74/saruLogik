import {ValueView} from './value-view';

export class LogikViewLine {
  type:string;
  hideSub:boolean;
  view: ValueView[];
  blockId:number;
  lineId:number;
  rightLineId:number;
  // relationId:number;
}
